# Simple Calculator

A basic command-line calculator written in Python that performs simple arithmetic operations such as addition, subtraction, multiplication, division, and modulus.

## 📋 Features

- Addition (`+`)
- Subtraction (`-`)
- Multiplication (`*`)
- Division (`/`)
- Modulus (`%`)

## 🚀 How to Run

1. **Clone or download** the project to your local machine.
2. Make sure you have **Python 3.x** installed.
3. Open a terminal or command prompt in the project directory.
4. Run the program using:

   ```bash
   python simple calculator.py
